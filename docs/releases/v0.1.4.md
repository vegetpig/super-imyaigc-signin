# v0.1.4 Skill 说明同步

这个版本把连续签到天数输出同步到 `SKILL.md`。

## 更新

- `SKILL.md` 说明 `signin.py` 支持全账号签到。
- `SKILL.md` 说明签到后会保存 `post-signin-*.png` 截图。
- `SKILL.md` 说明签到后会输出 `Consecutive sign-in days: X`。
- 验证清单新增 `signin.py --retries 1` 的多账号签到和连续天数检查。
